"""
Show the complexity of the operations in the
different data structures

+ list:
    1. index -> O(1)
    2. sort -> O(n * log(n)) with tim sort
    3. append -> O(1)
    4. pop -> O(1)
    4. pop(index_k) -> O(n) in the worst case
    5. insert -> O(n) in the worst case
    7. in -> O(n)
    8. extend -> O(k: iter)
    9. count -> 0(n)
+ tuple:
    1. index
    2. count
    3. __add__
    4. __contains__
+ dict:
    1. items -> O(n)
    2. keys -> O(n)
    3. values -> O(n)
    4. pop ->
    5. get ->
    6. fromkeys ->
    7. setdefault ->
"""